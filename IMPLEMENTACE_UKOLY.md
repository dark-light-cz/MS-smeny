# Seznam implementačních úkolů

Postupná implementace funkcí a částí aplikace MS-smeny. Úkoly plníme v pořadí (nebo v logických blocích); po každém dokončeném úkolu aktualizujeme AKTUALNI_STAV.md.

---

## Blok A: Data a kostra aplikace

| # | Úkol | Stav | Poznámka |
|---|------|------|----------|
| A1 | **Datový model a Local Storage** – definice struktury dat (zaměstnanci: id, jméno, úvazek v minutách/týden, role; budovy, třídy, otevírací doby, pravidla). Načtení a uložení do Local Storage při změně. | [x] | data-model.js, storage.js; načtení při startu, setData/replaceData/ulozNyni. |
| A2 | **Export a import JSON** – funkce exportu celých dat do JSON souboru a importu z JSON (obnovení dat). Bez UI zatím jen volatelné z konzole nebo později napojit na tlačítka. | [x] | export-import.js: exportData(), stahnoutExport(), importZeJSON(); test-export-import.js. |
| A3 | **Kostra stránky a navigace** – základní rozložení aplikace: hlavička, navigace mezi sekcemi (např. Přehled / Zaměstnanci / Budovy a třídy / Pravidla / Návrh směn), obsahová oblast. Jedna stránka, sekce přepínané v JS (ne více HTML). | [x] | index.html: hlavička + nav + 5 sekcí; js/navigace.js: zobrazSekci, hash; test-navigace.js. |

---

## Blok B: Konfigurace – základy

| # | Úkol | Stav | Poznámka |
|---|------|------|----------|
| B1 | **Zaměstnanci – seznam a formulář** – zobrazení seznamu zaměstnanců, přidat / upravit / smazat. Pole: jméno, úvazek (hodiny + minuty za týden), role (výběr: učitelka, asistentka pedagoga, školník/školnice, ředitelka, zástupkyně). Ukládání do modelu a Local Storage. | [x] | Sekce Zaměstnanci: tabulka, formulář, tlačítka; js/zamestnanci.js; test-zamestnanci.js. |
| B1b | **Zaměstnanci – řazení tabulky** – na pohledu Zaměstnanci automaticky řadit tabulku: nejdřív podle role (pořadí: ředitelka, zástupkyně, učitelka, asistentka pedagoga, školník/školnice), v rámci role podle jména. | [x] | seradZamestnance() v zamestnanci.js, vykresliSeznam volá řazení; test-zamestnanci.js. |
| B1c | **Zaměstnanci – řazení sloupců uživatelem** – u tabulky zaměstnanců umožnit řazení podle sloupců zvolených uživatelem; podpora řazení podle více parametrů (více sloupců). Jednoduchá vizualizace ikonkou (např. šipka nahoru/dolů podle směru, případně pořadí kritérií); ovládání co nejběžnější a uživatelsky známé (typicky klik na záhlaví sloupce, případně Shift+klik pro sekundární řazení). | [x] | Klik = primární řazení (toggle směr), Shift+klik = přidat kritérium. Sloupce Jméno, Úvazek, Role, Kategorie. Ikona ▲/▼ a pořadí 1,2. js/zamestnanci.js. |
| B1d | **Zaměstnanci – nedostupnost v rámci týdne** – u každého zaměstnance umožnit určit časové období v rámci týdne, kdy nemůže sloužit (např. celý den, nebo jen dopoledne/odpoledne). Účel: částečné úvazky – zaměstnanec může jen vybrané dny (ostatní je jinde); ředitelka/zástupkyně – vyhrazený čas pro komunikaci s rodiči; rodinné důvody – nemůže ráno či odpoledne. Uložení do modelu, výpočet směn tyto intervaly respektuje. | [x] | Konfigurace: model (nedostupnost: [{den,od,do}]), UI (přidat/odebrat bloky, Celý den), tabulka, export/import. **Algoritmus respektuje** nedostupnost: buildAvailMask + longestAvailBlock → placeShifts nepoloží směnu do nedostupných minut, denní minuty se přerozdělí proporčně podle dostupného času. js/vypocet-smen.js, test-vypocet-smen.js (8 nových testů). |
| B1e | **Zaměstnanci – přiřazení jen do konkrétní budovy/třídy** – u zaměstnance v nastavení umožnit volitelně vybrat konkrétní budovu nebo konkrétní třídu; pokud je tak učiněno, zaměstnanec smí být přiřazen pouze do této budovy (příp. do libovolné třídy v ní) nebo pouze do této třídy. Výpočet směn toto omezení respektuje. **Validace** na záložce Návrh směn musí zahrnovat kontrolu tohoto pravidla (zaměstnanec s omezením nesmí být v návrhu přiřazen jinde). | [x] | Model: zamestnanec.prirazenoJen (null \| 'b:budovaId' \| 't:tridaId'). Formulář: select „Přiřazen pouze do“ (žádné, budovy, třídy). Tabulka: sloupec. Export/import normalizuje dle platných id. Výpočet: canAssignEmpToLocation v assignLocations. Validace: kontrola segmentů vůči prirazenoJen (validace-navrhu.js). |
| B2 | **Budovy a třídy – struktura** – CRUD budov, v každé budově CRUD tříd. U budovy/třídy: název. Zobrazení hierarchie (budova → třídy). Ukládání do modelu a Local Storage. | [x] | Sekce Budovy a třídy: hierarchie, formuláře budov/tříd; js/budovy-tridy.js; test-budovy-tridy.js. |
| B3 | **Otevírací doba** – u budovy nebo třídy nastavení otevírací doby (např. den v týdnu + od–do, nebo jednoduchý model „po–pá 7:00–17:00“). Uložení do modelu. | [x] | Formuláře budov a tříd: fieldset Otevírací doba (dny Po–Ne, od/do); třída má oteviraciDoba v modelu; import doplní chybějící. |
| B4 | **Min/max počet osob v čase** – konfigurace časových slotů (od–do) a pro každý slot min/max počet osob (na třídu nebo na budovu). Příklad: 7:00–7:45 min 1 na budovu; od 7:45 min 1 na třídu. Uložení do modelu. | [x] | Sekce Pravidla: časové sloty, formulář s nápovědami, validace, zprávy bez alertů; js/min-max-sloty.js, vytvorMinMaxSlot v modelu; test-min-max-sloty.js. |
| B4b | **Požadavky na počet osob – duplikovat řádek** – v tabulce „Požadavky na počet osob v čase“ přidat možnost duplikovat řádek: obsah řádku se předvyplní do formuláře nového záznamu (uživatel může upravit a uložit jako nový slot). | [x] | Tlačítko „Duplikovat“ u každého řádku; otevře formulář s předvyplněnými hodnotami (bez id) pro uložení jako nový slot. js/min-max-sloty.js. |

---

## Blok C: Konfigurace – pokročilá pravidla

| # | Úkol | Stav | Poznámka |
|---|------|------|----------|
| C1 | **Minimální překryv v třídě** – konfigurovatelná délka (např. 2 hodiny) překryvu dvou pedagogů v každé třídě každý den. Uložení do modelu. | [x] | Sekce Pravidla: blok „Minimální překryv v třídě“, formulář (hodiny), js/pravidla-prekryv.js; pravidla.minimalniPrekryvMinuty; test-pravidla-prekryv.js. |
| C2 | **Kmenové vs. vykrývací** – u zaměstnance označení kmenová/vykrývací; přiřazení kmenových k jedné třídě (2–3 na třídu). Pravidlo „bez mezer“, „max 1 přesun“ u vykrývací – uložení do modelu, výpočet využije později. | [x] | Model: kmenovaVykryvaci, tridaId; pravidla: vykryvaciBezMezer, vykryvaciMaxPresun, min/maxKmenovychNaTridu. Formulář zaměstnance: kategorie + select třídy. Sekce Pravidla: blok „Pravidla pro vykrývací“. Export/import doplňuje a sloučí. |
| C3 | **Speciální sloty a rotace** – definice slotů se sníženým počtem osob (např. pátek 15:30–17, 1 osoba na budovu) a příznak „požadovat střídání“ (rotace). Uložení do modelu. | [x] | Slot: volitelná pole dny (1–5), rotace (boolean). Formulář: výběr dnů (Po–Pá), checkbox rotace. Tabulka: sloupce Dny, Rotace. Import normalizuje dny/rotace. |
| C4 | **Omezení „ne dohromady“** (volitelně později) – výběr dvojic osob, které nemají být spolu v jedné třídě/směně. Uložení do modelu. | [x] | Model: omezeniNeDohromady [], vytvorOmezeniNeDohromady(id1, id2). Sekce Pravidla: blok s výběrem dvou osob, seznam dvojic, smazat. Import normalizuje. js/omezeni-ne-dohromady.js. |
| C5 | **Konfigurace přechodu mezi budovami** – globální nastavení: zakázat přechod mezi budovami (zaměstnanec v jednom dni jen v jedné budově). U zaměstnance lokální nastavení: výchozí (dodrží globální) / zakázat / povolit. Implementace konfigurace: model (pravidla + pole u zaměstnance), UI (globální v Pravidla, u zaměstnance výběr), export/import. | [x] | Model: pravidla.zakazPrechodMeziBudovami (bool, výchozí true), zamestnanec.prechodMeziBudovami ('výchozí'/'zakázat'/'povolit'). UI: sekce Pravidla – blok „Přechod mezi budovami" (checkbox + uložit); formulář zaměstnance – select. Tabulka zaměstnanců: sloupec „Přechod budovy". Export/import normalizuje oba nové fieldy. **Algoritmus zatím neaplikuje** – viz D5. |
| C6 | **Konfigurace: střídání dopoledne/odpoledne** – globální pravidlo: zapnuto/vypnuto a **režim tvrdý / preferenční** (v konfiguraci). Tvrdý = zaměstnanec nesmí mít každý den stejný typ směny (např. vždy od 7:00); preferenční = algoritmus to jen preferuje. Cíl: tentýž zaměstnanec střídá dopolední a odpolední směny mezi dny. Definice dopoledne/odpoledne: např. hranice 12:00 (konfigurovatelné nebo pevné). Model + UI v Pravidla, export/import. | [x] | Model: stridaniDopoledneOdpoledne, stridaniRezim, stridaniHraniceMinuty. UI: blok v Pravidla (checkbox, select, time). Export/import normalizuje. **Algoritmus zatím neaplikuje** – viz D6. |
| C7 | **Konfigurace: preferovat souvislé bloky a méně dnů** – globální pravidlo (zapnuto/vypnuto, volitelně min. délka bloku v minutách): preferovat u kratších úvazků souvislé bloky pracovní doby a koncentraci do méně pracovních dnů (některé dny volno) místo jednoho krátkého bloku v každém dnu. Model + UI v Pravidla, export/import. | [x] | Model: preferSouvisleBlok, minDelkaBlokuMinuty. UI: blok v Pravidla (checkbox, input min délka). Export/import normalizuje. **Algoritmus zatím neaplikuje** – viz D7. |

---

## Blok D: Výpočet a zobrazení návrhu

| # | Úkol | Stav | Poznámka |
|---|------|------|----------|
| D1 | **Výpočet – první verze** – algoritmus: na vstupu konfigurace (zaměstnanci, budovy, třídy, otevírací doba, min/max v čase). Výstup: jedno řešení – přiřazení „kdo kdy kde“ tak, aby byly splněny min/max požadavky a úvazky. Zjednodušená verze bez překryvu, kmenových a rotace je OK. | [x] | js/vypocet-smen.js: vypocetSmen(data) → { ok, prirazeni } nebo { ok: false, chyba }. Přepsáno 2026-02-05: 4fázový algoritmus (poptávka → plánování směn → přiřazení míst → segmenty). Sloty = kontrolní pravidla (ne přiřazení). **Upřesnění 2026-02-06:** zaměstnanec je vždy v konkrétní třídě (ne „na budově bez třídy"); požadavek na budovu se plní přes přítomnost v třídě dané budovy. **2026-02-06:** algoritmus respektuje nedostupnost zaměstnanců (B1d) – maska dostupnosti, přerozdělení úvazku proporčně. |
| D2 | **Zobrazení návrhu směn** – stránka/sekce „Návrh směn“: zobrazení výsledku výpočtu (tabulka nebo přehled podle dnů/tříd/osob). Tlačítko „Přepočítat“. | [x] | Sekce Návrh směn: tlačítko Přepočítat, tabulka Den | Čas | Místo | Osoby. js/navrh-smen-ui.js. |
| D2c | **Export výsledku návrhu směn jako CSV** – možnost stáhnout aktuální návrh (tabulka Den, Čas, Místo, Osoby) jako CSV soubor. Tlačítko v sekci Návrh směn (aktivní po přepočtu). | [x] | js/export-navrh-csv.js: navrhToCsv(), stahnoutNavrhCsv(); tlačítko „Stáhnout CSV“; test-export-navrh-csv.js. |
| D2b | **Grafické zobrazení návrhu pracovní doby** – zobrazit výsledný návrh pracovní doby graficky. Struktura: každá budova = vlastní blok; v bloku každá třída = vlastní řádek. Řádek = časová osa (např. vodorovně), na ní barevné obdélníky = jednotlivé učitelky (přiřazení v daném čase). Při najetí myší (popup/tooltip) zobrazit od kdy do kdy (časový rozsah) a jméno učitelky. Volitelně: legenda barev (kdo jakou barvu má), zobrazení po dnech (jeden den = jedna „síť“ budov/tříd). | [x] | js/navrh-graf.js: bloky budov, řádky tříd/budova, časová osa, barvy, tooltip, legenda, výběr dne; test-navrh-graf.js. |
| D2d | **Graf návrhu: záložky (taby) a režim inline** – místo selectu pro výběr dne zobrazit **záložky (taby)** – jeden den = jedna záložka. Přidat **tlačítko** přepínající režim zobrazení: **„Taby“** (jeden den na obrazovce, přepínání záložkami) vs. **„Inline“** (všechny dny pěkně pod sebou na jedné stránce). | [x] | navrh-graf.js: ovládací prvky (Taby/Inline), taby Po–Pá, buildObsahJednohoDne; režim inline = 5 sekcí pod sebou. navrh-smen-ui.js: stav grafRezim, grafVybranyDen, callbacky onDenChange/onRezimChange. CSS: .navrh-graf-ovladaci, .navrh-graf-rezim, .navrh-graf-taby, .navrh-graf-inline. |
| D3 | **Rozšíření výpočtu** – zapojení dalších pravidel: minimální překryv v třídě, kmenové/vykrývací (bez mezer, max 1 přesun), rotace ve speciálních slotech. | [x] | Překryv: syntetický slot minNaTridu 2 (pravidla.minimalniPrekryvMinuty). Kmenová preferována u své třídy; vykrývací max N tříd/den (vykryvaciMaxPresun). Rotace: u slot.rotace preferovat méně často přiřazené. js/vypocet-smen.js, zobrazení překryvu v UI. **⚠ Pravidlo vykryvaciBezMezer se v algoritmu nepoužívá — bude součástí přepisu D1.** |
| D4 | **Plánování na týden vs. 14 dní** – pedagogové týdenní opakující se směny; školníci volitelně plán na 14 dní (krátký–dlouhý týden). Zapojit do výpočtu a zobrazení. | [ ] | |
| D5 | **Aplikace přechodu mezi budovami ve výpočtu** – algoritmus výpočtu směn respektuje globální „zakázat přechod mezi budovami“ (C5) a per-zaměstnanec nastavení (výchozí/zakázat/povolit); zaměstnanec není přiřazen do více budov v tentýž den, pokud to nastavení vylučuje. | [x] | maZakazPrechodu helper, canGoToBuilding v assignLocations; doAssign trackuje budovu; kroky 3–5 filtrují dle budovy; 5 unit testů + 3 integrační. |
| D6 | **Aplikace střídání dopoledne/odpoledne ve výpočtu** – pokud je zapnuto pravidlo (C6), algoritmus respektuje zvolený režim: **tvrdý** = nepřipustit řešení, kde tentýž zaměstnanec má každý den stejný typ směny; **preferenční** = při výběru řešení preferovat střídání dopoledne/odpoledne mezi dny. | [x] | getShiftTypeFromSegmenty, validujStridaniTvrdy; placeShifts(stridaniOpt); preferenční: preferredType z předchozích dnů; tvrdý: validace po výpočtu. test-vypocet-smen.js. |
| D7 | **Aplikace souvislých bloků a méně dnů ve výpočtu** – pokud je zapnuto pravidlo (C7), algoritmus u kratších úvazků preferuje rozvržení do méně dnů s delšími souvislými bloky (některé dny bez přiřazení) místo krátkých bloků rozptýlených po každém dnu. Volitelně respektovat min. délku bloku z konfigurace. | [x] | empDailyPrecomputed pro úvazek < 1200 min/týden; dny řazeny podle maxBlock; minDelkaBlokuMinuty přeskočí příliš krátký blok. test-vypocet-smen.js. |
| D1alt | **Algoritmus „Párové třídy“** – alternativní výpočet: na každé třídě dvojice učitelek (ideálně kmenové) pokrývající 7:45–16:00 se střídáním ranní/odpolední; doplnění nedostatku úvazků; ranní 7:00 a večerní 17:00 na budovu se střídáním; vykrývací podle budov; dočerpání úvazků. | [x] | Specifikace: **ALGORITMUS-PAROVE-TRIDY-SPEC.md**. Implementace: js/algoritmy/parove-tridy.js (fáze 1–3, varování fáze 4), registrace v registr.js. Půlení s překryvem: dopolední 7:45–12, odpolední 10–16. Jednotné 7:45–16:00 pro všechny třídy. Testy: test-parove-tridy.js. |
| — | **Známé omezení:** Slot s minNaTridu ≥ 2 v daném časovém okně (např. 9:30–11:00) – algoritmus někdy neumístí do dané třídy v daném čase dost osob. Regresní test v test-vypocet-smen.js („Slot 09:30–11:00 minNaTridu 2…“) to odhalí; oprava zatím otevřená (viz OPEN_QUESTIONS.md bod 2). | — | |
| D8 | **Tabulka Den–Zaměstnanec–Čas–Místo rozbalovací** – na záložce Návrh směn bude tabulka s přehledem v rozbalovací sekci; **výchozí stav skrytá** (sbalená). Uživatel ji může rozbalit podle potřeby. | [x] | index.html: `<details id="navrh-tabulka-details">` s výchozím sbalením; css/main.css: styly pro rozbalovací sekci. |
| D9 | **Import návrhu ze CSV** – vedle stahování výsledku v CSV umožnit **nahrát** soubor CSV (import návrhu); načtená data se zobrazí v tabulce a v grafu stejně jako po přepočtu. Formát odpovídá exportu (Den, Zaměstnanec, Čas, Místo). | [x] | Tlačítko „Nahrát CSV“, file input; js/import-navrh-csv.js: csvToPrirazeni(); navrh-smen-ui.js: nahratCsv, onCsvFileSelected; test-import-navrh-csv.js. |
| D10 | **Tlačítko Validovat a tabulka chyb/varování** – tlačítko „Validovat“ zkontroluje, zda aktuální vygenerovaný (nebo načtený) návrh splňuje všechna pravidla. Pokud ne: zobrazí **přehlednou tabulku**, kde a co nesedí (porušené pravidlo, kontext). Zahrnuje i **přečerpaný a nevyčerpaný úvazek** (kdo má víc/méně hodin než má). | [x] | Tlačítko „Validovat“, #navrh-validace-vysledek; js/validace-navrhu.js: validujNavrh (úvazky); tabulka chyb/varování; test-validace-navrhu.js. |
| D10b | **Validace: min/max na třídu a budovu** – rozšířit validaci na záložce Návrh směn o kontrolu **Požadavků na počet osob v čase** (Pravidla): v každém definovaném časovém slotu zkontrolovat, že v každé třídě/budově (podle typu slotu) je počet osob v rozsahu min–max. Porušení zobrazit v tabulce chyb/varování (den, čas, místo, očekáváno vs. skutečnost). | [x] | js/validace-navrhu.js: minMaxPocetOsobVTridVCase, minMaxPocetOsobVBudoveVCase; validujNavrh bere data.budovy a data.minMaxSloty, pro každý slot a den kontroluje min/max na třídu a budovu; polozky „Min/max na třídu“, „Min/max na budovu“. Testy v test-validace-navrhu.js. |
| D11 | **Editace bloků přímo na grafu** – nad grafem: klik na jednotlivé „oko“ (blok přiřazení) umožní **editovat** (změnit čas od–do), **přesunout** do jiné třídy (drag & drop), **smazat**. Po každé editaci se aktualizuje tabulka s chybami a varováními (D10). | [x] | Graf: data atributy na segmentech a řádcích; opts onSegmentClick, onSegmentDrop; modal „Upravit blok“ (čas, přesunout do, Smazat); navrh-smen-ui: applyNavrhChange, handleSegmentClick/Drop. |
| D12 | **Doplnění nevyčerpaného úvazku z tabulky chyb** – v tabulce chyb/varování: klik na **jméno** u položky „nevyčerpaný úvazek“ otevře **popup**, kde uživatel zadá den, čas od–do a třídu. Při výběru **času od** se **čas do** automaticky přepočítá: min(dostupný zbývající čas, 8 h), případně zkráceno na max otevírací dobu třídy. Po uložení se návrh a tabulka chyb aktualizují. | [x] | Validace: polozky mají zamestnanecId; v tabulce klikatelné jméno u „Nevyčerpaný úvazek“; modal „Doplnit úvazek“ (den, čas od/do, třída); updateDoplnitCasDo(); saveDoplnitUvazek. |
| D10c | **Po Přepočítat automaticky Validovat a chybový report pro AI** – po kliku na „Přepočítat“ se rovnou vyvolá validace; pokud dojde k chybě/varování, zobrazí se tlačítko „Stáhnout chybový report pro AI“. Report obsahuje anonymizovanou **Konfiguraci**, **Vygenerovaný přehled** a **Nalezené chyby** (jména → ucitelka1, budova1, trida1 atd.). Dokumentace **CHYBOVY_REPORT.md** popisuje, jak report předat AI a jak postupovat (1. test odhalí chybu, 2. úprava kódu). | [x] | js/chybovy-report.js: buildAnonymizer, generateReport, stahnoutChybovyReport; navrh-smen-ui: po Přepočítat volá validujNavrh a validovat(result), při chybách tlačítko Stáhnout chybový report; CHYBOVY_REPORT.md. |
| D2e | **Barvy učitelek v grafu návrhu** – přiřadit barvy učitelkám (v nastavení Zaměstnanci: volitelně barva u každého; defaultně nevyužitá z palety). Při načtení stránky/konfigurace doplnit chybějící barvy a uložit. Legenda: v režimu inline stačí jedna legenda (ne u každého dne). | [x] | Model: zamestnanec.barva (hex); doplnBarvyZamestnancum() v navrh-graf.js (volá se z main.js po nacti() a po importu); formulář Zaměstnanci: pole Barva (input type=color); export/import zachová barva; jedna legenda pro inline. |

---

## Blok E: Dokončení a UX

| # | Úkol | Stav | Poznámka |
|---|------|------|----------|
| E1 | **Export/import – UI** – tlačítka nebo menu „Exportovat data“ (stáhnout JSON) a „Importovat data“ (nahrát JSON, nahradit/ sloučit dle dohody). | [x] | Sekce Přehled: blok Záloha a obnova dat, tlačítka Exportovat data / Importovat data; js/export-import-ui.js; po importu obnova pohledů. |
| E1b | **Import zálohy přes drag & drop** – umožnit načíst zálohu (konfiguraci) přetažením souboru (JSON) nad oblast „Záloha a obnova dat“; po puštění souboru provést import stejně jako u tlačítka Importovat data. | [ ] | |
| E2 | **Validace a hlášení chyb** – kontrola vstupů (úvazek, časy, povinná pole), zobrazení srozumitelných hlášek. Při výpočtu např. „nelze najít řešení“ + důvod pokud možno. | [ ] | |
| E3 | **Doladění UX** – konzistentní styly, přehlednost formulářů a tabulek, základní responzivita. | [ ] | |
| E4 | **Zobrazovaný název aplikace** – upravit lidský název aplikace zobrazený v hlavičce stránky a v titulku prohlížeče (`<title>`). Jde o to, jak se aplikace jmenuje pro uživatele v UI, ne o název repozitáře na GitHubu. | [ ] | |
| E5 | **Vylepšit vzhled** – vzhled aplikace: barevné schéma, typografie, vizuální hierarchie a čitelnost; konzistentní vzhled formulářů, tabulek a navigace tak, aby byl celkový dojem přehledný a přívětivý. | [ ] | |
| E6 | **Nápověda na stránkách nastavení** – zrevidovat a doplnit nápovědu k jednotlivým sekcím a polím (Zaměstnanci, Budovy a třídy, Pravidla) tak, aby bylo nastavení srozumitelné i pro laiky (neodborníky). | [ ] | |

---

## Jak s dokumentem pracovat

- **Zaškrtnutí:** po dokončení úkolu změň `[ ]` na `[x]` a doplň krátkou poznámku.
- **Po každém dokončeném úkolu:** aktualizuj **AKTUALNI_STAV.md** – sekci „Co aplikace aktuálně umí“.
- **Testy:** před uzavřením úkolu spusť `npm test`. Pro novou funkci přidej nové testy (v příslušném souboru v `test/` nebo nový testovací soubor). Existující testy měň jen po zdůvodnění a po potvrzení uživatelem – viz pravidla v `.cursor/rules/projekt.mdc`.
- **Odkaz z CHECKLIST.md:** Fáze 2 (Implementace jádra) se plní podle tohoto seznamu; úkol 2.1 = např. A1–A3, pak B1.
